<!DOCTYPE html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
  <title><%= directory %></title>
  <style>
    body { font-family: "Helvetica Neue", Helvetica, Arial, sans-serif; font-weight:300; background: #CCC; }
    .content { margin-top: 40px; width: 900px; border-radius: 8px; background: #EEE; moz-box-shadow: 0px 1px 0px #FFF; -webkit-box-shadow: 0px 1px 0px #FFF; box-shadow: 0px 1px 0px #FFF; font-size: 22px;border: 1px Solid #AAA;}
    ul { margin: 0; padding: 0; display: block; border-top: 1px solid #CCC;}
    ul li { list-style: none; text-align: left; }
    ul li a { color: #333; display: block; padding: 10px; font-size: 14px; border-bottom: 1px solid #CCC; text-decoration: none; background: #EEE; }
    ul li a:hover { background: #FFF;}
    .info { font-size: 12px; text-shadow: 1px 1px 1px #FFF;}
    .back { text-align: left; margin-left: 20px; }
    .back a { padding: 8px; background: #333; color: #FFF; border-radius: 6px; margin-top: 10px; text-decoration: none; font-size: 14px; }
    .back a:hover { background: #888; }
  </style>
<head>
<body>
    <div align="center">
      <div class="content">
        <p><%= directory %></p>
        <ul>
          <% directories.each_with_index do |d,i| %>
          <li><a href="<%= d %>"><%= d %></a></li>
          <% end %>
        </ul>
        <div class="back">
          <p><a href="#" onclick="javascript: history.go(-1)">&larr; back</a></p>
        </div>
      </div>
      <p class="info">
        <i>Cuda Server version <%= Cuda::VERSION %> - <%= DateTime.now.httpdate %></i>
      </p>
    </div>
</body>
</html>